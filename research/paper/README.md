# Paper

The two-page CVPR-style source is `main.tex` with `cvpr.sty` and
`ieeenat_fullname.bst`.

Compile with Tectonic:

```bash
tectonic -X compile main.tex
```

The compiled two-page submission is `main.pdf`.  The final submission PDF is
`Bolin_Tao.pdf` at the repository root and is generated from the official CVPR
2026 template under `CVPR_2026_Submission_Template/`.
